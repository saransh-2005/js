"use strict"; // treats all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser.it works in browser

console.log(3 
    +
     3) // code readability should be high .it will work but it doesn't look good and difficult to understand

console.log("Hitesh")


let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined =>   means value is not assigned yet
// symbol => unique


// object

console.log(typeof undefined); // output will be undefined as it is a datatype
console.log(typeof null); // output will be object as null is a object